 * FORMATING
 * 
 * DATA:
 * String: "text"
 * Int: 1234
 * Double: 12.34
 * Array: {<data>,Ect.}
 * Variable: %ExampleVar% --Varible names can NOT contain: '%' or ' '
 * VaribleArray: %ExampleArray%[<Int>] --Gets the data from that location in the array. it will return "OUTOFBOUNDS" if the location does not exist
 * 
 * MATH:
 * = --Used for setting a varible's data
 * - --Used for subtraction
 * + --Used for addition. Unless used with a String then it combinds the String with the other data
 * * --Used for multiplication
 * / --Used for division
 * 
 * OPERATORS:
 * == --Used for checking if two varibles are the same
 * & --Used for making sure multiple logic parts are both true
 * | --Used for checking if one of the logic parts is true
 * 
 * Functions:
 * NewVar <VarName>  --A Unsigned varible
 * NewVar <VarName> Array --A unsigned varible array
 * NewVar <VarName> <Type> --A varible with a type but no data
 * NewVar <VarName> <Type> As Array --A varible array with a type but no data
 * NewVar <VarName> <Type> = <Data> --A varible with a type and data
 * NewVar <VarName> <Type> As Array = <Array> --A varible array with a type and data
 * 
 * Var <Varible> <Math> <Data/Variable> --Changes that varible's data
 * Var <Varible>[<Int/Varible>] <Math> <Data/Variable> --Changes the data at that location
 * Var <VaribleArray> <Math> <Data/Varible/VariableArray> --Will do math functions with each item or do a math function with all the items
 * 
 * <Varible> As <Type> --Temporarily converts that varible to another type unless it's being used for defining a Array's type
 * Asign <Variable> <Type> --Changes the variable's type
 * Asign <Variable> Array As <Type> --Changes the variable array's type
 * 
 * Function <FuncName> --Creates a function without any parametors
 * Function <FuncName> <Array> --Creates a function with parametors
 * EndFunction --Ends the  function
 * 
 * FUNCTIONS.CONSOLE:
 * Print <Data/Varible/VaribleArray> --Prints data to the console. In the case of a Array being used it will list every item in the array
 * PrintLn <Data/Varible/VaribleArray> --Prints data to the console then creates a new line. In the case of a Array being used it will list every item in the array on a new line
 *
